import { Route, Routes } from "react-router-dom";

function App() {
  return (
    <div className="App">
      {/* <Routes>
        <Route exact path="/" element={<LandingPage />} />
      </Routes> */}
      APP화면이 보일 자리
      <br />
      여기에 라우팅해줄 예정
    </div>
  );
}

export default App;
